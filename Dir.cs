using System;
namespace BroadcastStorm
{
    public enum Dir
    {
        /// <summary> 
        /// Dir is an Enumeration indicating Direction, and is used between the
        /// HandleInput Class and Player
        /// /// <summary> 
        
        Up,
        Down,
        Left,
        Right
    }
}
